# Best Practices

- Add detailed console logs for each step in scripts
- Use structured JSON output for programmatic results
- Include `main().catch()` blocks for graceful error handling
- Avoid over-reliance on external libraries for simple tasks
- Fix stale references when switching libraries/tools
